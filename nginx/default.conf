server {
    listen 80;

    location / {
        proxy_pass http://app:3000;
        add_header Cache-Control "public, max-age=60";
    }

    location /health {
        proxy_pass http://app:3000;
        add_header Cache-Control "no-cache";
    }
}
